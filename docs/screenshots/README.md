# Screenshots

| File | Used by |
|---|---|
| `sensei-hero.png` | README hero illustration (top of `README.md`) |

The four operator/projector captures that an earlier version of this guide
listed (`display-flow-paper.png`, `operator-console.png`, `live-recording.png`,
`extend-card.png`) were never captured before the hackathon deadline. If you
take them, drop them here at 1920 x 1080 and add a `## Screenshots` section
back to the README; the filenames are free.

Capture tips: Windows Snipping Tool (`Win+Shift+S`, Window mode) or the browser
DevTools device toolbar; PNG, ideally under 500 KB (`pngquant` / tinypng).
