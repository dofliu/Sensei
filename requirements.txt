# Sensei dependencies — Python 3.12 (the version the project is developed on)
# ------------------------------------------------
# Prereq (one-time, NOT in pip):
#   1. Install Ollama from https://ollama.com  (Windows/Mac/Linux)
#   2. ollama pull gemma4:e2b   (or gemma4:e4b)
#
# Then for PyTorch (Faster-Whisper needs it; CUDA build for GPU):
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
#
# Then:
#   pip install -r requirements.txt
#
# Upper bounds are deliberate: Gradio 6 already broke the 5.x API once
# (theme/css moved off the Blocks constructor). Bump them on purpose, not by
# accident on a fresh install.

# ASR
faster-whisper>=1.0.3,<2

# LLM (Ollama Python client; talks to local Ollama daemon)
ollama>=0.3.0,<1

# Schema validation
pydantic>=2.6.0,<3

# UI (Gradio 6 API; the FastAPI mount for /display comes with it)
gradio>=6.0,<7

# Audio I/O (live mic mode)
sounddevice>=0.4.6,<1
soundfile>=0.12.1,<1
numpy>=1.26,<3
